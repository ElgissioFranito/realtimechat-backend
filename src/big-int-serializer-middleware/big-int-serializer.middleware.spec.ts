import { BigIntSerializerMiddlewareMiddleware } from './big-int-serializer.middleware';

describe('BigIntSerializerMiddlewareMiddleware', () => {
  it('should be defined', () => {
    expect(new BigIntSerializerMiddlewareMiddleware()).toBeDefined();
  });
});
